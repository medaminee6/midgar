<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz de Préférences - Midgar</title>
<<<<<<< HEAD
    <link rel="stylesheet" href="/css/midgar.css">

    <!-- QUIZ OPTIONS STYLING -->
    <style>
        /* === QUIZ OPTIONS GRID === */
=======
    
    <style>
        /* ===== VARIABLES & RESET ===== */
        :root {
            --primary-color: #18E3A4;
            --primary-dark: #13c891;
            --text-color: #E6FFF6;
            --text-secondary: #B0B9B6;
            --bg-primary: #1A1F1E;
            --bg-dark: #0D0F0F;
            --card-bg: rgba(26, 31, 30, 0.7);
            --glass-bg: rgba(26, 31, 30, 0.5);
            --border-radius: 12px;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }
        
        body {
            color: var(--text-color);
            line-height: 1.6;
            overflow-x: hidden;
        }
        
        a {
            text-decoration: none;
            color: inherit;
        }
        
        ul {
            list-style: none;
        }
        
        .container {
            width: 90%;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        /* ===== BOUTONS ANIMÉS ===== */
        .btn {
            display: inline-block;
            padding: 0.6rem 1.2rem;
            border-radius: var(--border-radius);
            font-weight: 600;
            transition: all 0.3s ease;
            text-align: center;
            border: none;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }
        
        .btn-primary {
            background-color: var(--primary-color);
            color: #000;
        }
        
        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: scale(1.05);
            box-shadow: 0 0 15px var(--primary-color);
        }
        
        .btn-secondary {
            background-color: transparent;
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
        }
        
        .btn-secondary:hover {
            background-color: var(--primary-color);
            color: #000;
            transform: scale(1.05);
            box-shadow: 0 0 15px var(--primary-color);
        }
        
        .btn-sm {
            padding: 0.4rem 0.8rem;
            font-size: 0.9rem;
        }
        
        /* ===== BOUTONS MAGIQUES (magic-btn) ===== */
        .magic-btn {
            position: relative;
            overflow: hidden;
            display: inline-block;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
            padding: 0.5rem 1rem;
            background: transparent;
            color: var(--text-color);
            border: 1px solid transparent;
        }
        
        .magic-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 0 15px var(--primary-color);
            border-color: var(--primary-color);
            color: var(--primary-color);
            background: rgba(24, 227, 164, 0.1);
        }
        
        .magic-btn.active {
            color: var(--primary-color);
            border-color: var(--primary-color);
            background: rgba(24, 227, 164, 0.1);
        }
        
        /* ===== ANIMATION PARTICULES ===== */
        .particle {
            position: absolute;
            width: 6px;
            height: 6px;
            background: radial-gradient(circle, var(--primary-color), var(--primary-dark));
            border-radius: 50%;
            pointer-events: none;
            animation: fly 0.6s forwards;
            opacity: 0.8;
            z-index: 1;
        }
        
        @keyframes fly {
            0% { 
                transform: translate(0, 0) scale(1); 
                opacity: 1; 
            }
            100% { 
                transform: translate(var(--x), var(--y)) scale(0); 
                opacity: 0; 
            }
        }
        
        /* ===== VIDEO BACKGROUND ===== */
        .video-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -2;
            pointer-events: none;
        }
        
        .video-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            z-index: -1;
        }
        
        .gradient-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--bg-primary) 0%, var(--bg-dark) 100%);
            z-index: -3;
            pointer-events: none;
            display: none;
        }
        
        /* ===== HEADER STANDARD ===== */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 100;
            background: rgba(26, 31, 30, 0.9);
            backdrop-filter: blur(10px);
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--primary-color);
            cursor: pointer;
        }
        
        .logo:hover {
            animation: glow 1.5s infinite;
        }
        
        .logo svg {
            width: 32px;
            height: 32px;
        }
        
        @keyframes glow {
            0%, 100% {
                text-shadow: 0 0 5px var(--primary-color);
            }
            50% {
                text-shadow: 0 0 15px var(--primary-color), 0 0 30px var(--primary-color);
            }
        }
        
        nav ul {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }
        
        .auth-buttons {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .profile-link {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 0.3rem 0.6rem;
            transition: all 0.3s ease;
            border-radius: 8px;
        }
        
        .profile-link:hover {
            background: rgba(24, 227, 164, 0.1);
        }
        
        .avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            overflow: hidden;
            border: 2px solid var(--primary-color);
            transition: transform 0.3s;
        }
        
        .avatar:hover {
            transform: scale(1.1);
            box-shadow: 0 0 10px var(--primary-color);
        }
        
        .avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        /* ===== SITE CONTENT ===== */
        .site-content {
            position: relative;
            z-index: 1;
            min-height: 100vh;
        }
        
        main {
            padding-top: 80px;
            min-height: 100vh;
        }
        
        .section {
            padding: 4rem 0;
            background-color: var(--glass-bg);
            border-radius: 16px;
            margin: 2rem auto;
            backdrop-filter: blur(10px);
            max-width: 1200px;
            border: 1px solid rgba(24, 227, 164, 0.1);
            transition: border-color 0.3s;
        }
        
        .section:hover {
            border-color: rgba(24, 227, 164, 0.3);
        }
        
        /* ===== STYLES SPÉCIFIQUES QUIZ ===== */
        .quiz-container { 
            background-color: var(--card-bg); 
            padding: 2rem; 
            border-radius: var(--border-radius); 
            border: 1px solid rgba(24, 227, 164, 0.1); 
            backdrop-filter: blur(10px);
        }
        
        .quiz-question h3 {
            color: var(--primary-color);
            margin-bottom: 1.5rem;
            font-size: 1.3rem;
        }
        
>>>>>>> validation-final-2
        .quiz-options {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
        }

        .quiz-options input[type="radio"] {
            display: none;
        }

        .quiz-option {
            position: relative;
            padding: 1.25rem 1.5rem;
<<<<<<< HEAD
            background-color: #0B0F0E;
            border: 2px solid #2A3139;
            border-radius: 10px;
            cursor: pointer;
            transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
            color: #E6FFF6;
=======
            background-color: rgba(0, 0, 0, 0.3);
            border: 2px solid rgba(24, 227, 164, 0.3);
            border-radius: var(--border-radius);
            cursor: pointer;
            transition: all 0.25s ease;
            color: var(--text-color);
>>>>>>> validation-final-2
            min-height: 90px;
            display: flex;
            align-items: center;
        }

        .quiz-option:hover {
            transform: translateY(-3px);
<<<<<<< HEAD
            border-color: #18E3A4;
=======
            border-color: var(--primary-color);
>>>>>>> validation-final-2
            box-shadow: 0 10px 25px rgba(24, 227, 164, 0.15);
        }

        .quiz-options input[type="radio"]:checked + .quiz-option {
            background: linear-gradient(135deg, rgba(24,227,164,0.14), rgba(24,227,164,0.05));
<<<<<<< HEAD
            border-color: #18E3A4;
=======
            border-color: var(--primary-color);
>>>>>>> validation-final-2
            box-shadow: 0 0 0 1px rgba(24,227,164,0.6), 0 12px 30px rgba(24,227,164,0.25);
        }

        .quiz-option::before {
            content: "";
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 4px;
<<<<<<< HEAD
            background: #18E3A4;
            opacity: 0;
            border-radius: 10px 0 0 10px;
=======
            background: var(--primary-color);
            opacity: 0;
            border-radius: var(--border-radius) 0 0 var(--border-radius);
>>>>>>> validation-final-2
            transition: opacity 0.25s ease;
        }

        .quiz-options input[type="radio"]:checked + .quiz-option::before {
            opacity: 1;
        }
<<<<<<< HEAD

        /* === PERSONALIZE FEED LINK === */
=======
        
        .progress-bar {
            background: rgba(0, 0, 0, 0.3);
            height: 4px;
            border-radius: 2px;
            overflow: hidden;
            margin-top: 2rem;
        }
        
        .progress-fill {
            background: linear-gradient(90deg, var(--primary-color), var(--primary-dark));
            height: 100%;
            width: {{ (step / total * 100) | round }}%;
            transition: width 0.3s ease;
        }
        
        .progress-text {
            text-align: center;
            font-size: 0.9rem;
            color: var(--text-secondary);
            margin-top: 0.5rem;
        }

>>>>>>> validation-final-2
        .personalize-link {
            text-align: center;
            margin-top: 1.5rem;
            font-size: 0.95rem;
<<<<<<< HEAD
            color: #B0B9B6;
        }

        .personalize-link a {
            color: #18E3A4;
            text-decoration: none;
            border-bottom: 1px solid rgba(24,227,164,0.4);
            padding-bottom: 2px;
            transition: color 0.25s ease, border-color 0.25s ease, text-shadow 0.25s ease;
        }

        .personalize-link a:hover {
            color: #E6FFF6;
            border-bottom-color: #18E3A4;
            text-shadow: 0 0 8px rgba(24,227,164,0.6);
        }

        @media (max-width: 640px) {
            .quiz-options {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<body>
<header>
    <div class="logo">
        <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
            <path d="M20,80 Q20,30 50,20 Q80,30 80,80 Z" fill="none" stroke="#18E3A4" stroke-width="2"/>
            <line x1="50" y1="20" x2="50" y2="50" stroke="#18E3A4" stroke-width="1.5"/>
            <circle cx="35" cy="40" r="3" fill="#18E3A4"/>
            <circle cx="65" cy="40" r="3" fill="#18E3A4"/>
            <circle cx="50" cy="60" r="4" fill="#18E3A4"/>
            <circle cx="50" cy="70" r="3" fill="#18E3A4"/>
        </svg>
        Midgar
    </div>

    <nav>
        <ul>
            <li><a href="/">Accueil</a></li>
            <li><a href="/discover">Découvrir</a></li>
            <li><a href="/universes">Univers</a></li>
            <li><a href="/oeuvre">Œuvre</a></li>
            <li><a href="/shop">Boutique</a></li>
            <li><a href="/challenges">Défis</a></li>
        </ul>
    </nav>

    <div class="auth-buttons">
        <a href="/login" class="btn btn-secondary btn-sm">Connexion</a>
        <a href="/register" class="btn btn-primary btn-sm">S'inscrire</a>
    </div>
</header>

<main>
<section class="section">
<div class="container">
<div style="max-width: 700px; margin: 0 auto;">

    <h1 class="text-center mb-4">Quiz de Préférences</h1>
    <p class="text-center mb-4" style="color:#B0B9B6;font-size:1.1rem;">
        Découvrez vos préférences en création fantasy
    </p>

    <div id="quiz-container" style="background-color:#1A1F1E;padding:2rem;border-radius:8px;border:1px solid #2A3139;">

        <div class="quiz-question">
            <h3 style="margin-bottom:1.5rem;">
                Quel type d'univers vous attire le plus ?
            </h3>

            <div class="quiz-options">
                <input type="radio" id="q1-epic" name="q1">
                <label for="q1-epic" class="quiz-option">
                    Épic et héroïque — Royaumes et légendes
                </label>

                <input type="radio" id="q1-dark" name="q1">
                <label for="q1-dark" class="quiz-option">
                    Sombre et mystérieux — Ombres et intrigues
                </label>

                <input type="radio" id="q1-magic" name="q1">
                <label for="q1-magic" class="quiz-option">
                    Magique et enchanteur — Merveilles et sorts
                </label>

                <input type="radio" id="q1-scifi" name="q1">
                <label for="q1-scifi" class="quiz-option">
                    Sci-Fi fantasy — Technologie & magie
                </label>
            </div>
        </div>

        <div style="margin-top:2rem;">
            <div style="background:#0B0F0E;height:4px;border-radius:2px;overflow:hidden;">
                <div style="background:linear-gradient(90deg,#18E3A4,#0DCF92);height:100%;width:25%;"></div>
            </div>
            <p style="text-align:center;font-size:0.9rem;color:#B0B9B6;margin-top:0.5rem;">
                Question 1 de 4
            </p>
        </div>

        <!-- NAVIGATION -->
        <div style="display:flex;gap:1rem;margin-top:2rem;justify-content:space-between;">
            <button class="btn btn-secondary" disabled>Précédent</button>
            <button class="btn btn-primary">Suivant</button>
        </div>

        <!-- PERSONALIZE FEED -->
        <div class="personalize-link">
            ou <a href="/preferences/advanced">personnaliser ton feed</a>
        </div>

    </div>

</div>
</div>
</section>
</main>

<footer>
    <div class="container">
        <div class="footer-bottom">
            <p>&copy; 2026 Midgar Fantasy Platform</p>
        </div>
    </div>
</footer>

<script src="/js/main.js"></script>
</body>
</html>
=======
            color: var(--text-secondary);
        }

        .personalize-link a {
            color: var(--primary-color);
            text-decoration: none;
            border-bottom: 1px solid rgba(24,227,164,0.4);
            padding-bottom: 2px;
            transition: all 0.25s ease;
        }

        .personalize-link a:hover {
            color: var(--text-color);
            border-bottom-color: var(--primary-color);
            text-shadow: 0 0 8px rgba(24,227,164,0.6);
        }
        
        /* ===== FOOTER ===== */
        footer {
            background-color: rgba(13, 15, 15, 0.5);
            padding: 3rem 0;
            margin-top: 4rem;
            border-radius: 16px;
            backdrop-filter: blur(8px);
            text-align: center;
            border-top: 1px solid rgba(24, 227, 164, 0.1);
        }
        
        footer:hover {
            border-top-color: rgba(24, 227, 164, 0.3);
        }
        
        .footer-bottom {
            text-align: center;
            color: var(--text-secondary);
            padding-top: 2rem;
            border-top: 1px solid rgba(24, 227, 164, 0.1);
        }
        
        /* ===== RESPONSIVE ===== */
        @media (max-width: 768px) {
            header {
                flex-direction: column;
                padding: 1rem;
                gap: 1rem;
            }
            
            nav ul {
                justify-content: center;
            }
            
            .auth-buttons {
                justify-content: center;
            }
            
            main {
                padding-top: 120px;
            }
            
            .section {
                margin: 1rem;
                padding: 2rem 0;
            }
            
            .quiz-options {
                grid-template-columns: 1fr;
            }
            
            .quiz-container {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- ===== BACKGROUND STANDARD ===== -->
    <div class="gradient-background" id="gradient-bg"></div>
    <div class="video-overlay"></div>
    <video id="bg-video" class="video-background" autoplay muted loop playsinline>
        <source src="{{ asset('uploads/dragon.mp4') }}" type="video/mp4">
        Votre navigateur ne supporte pas la vidéo HTML5.
    </video>

    <!-- ===== SITE CONTENT ===== -->
    <div class="site-content">
        <!-- ===== HEADER STANDARD AVEC MAGIC-BTN ===== -->
        <header>
            <div class="logo">
                <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
                    <path d="M20,80 Q20,30 50,20 Q80,30 80,80 Z" fill="none" stroke="#18E3A4" stroke-width="2"/>
                    <line x1="50" y1="20" x2="50" y2="50" stroke="#18E3A4" stroke-width="1.5"/>
                    <circle cx="35" cy="40" r="3" fill="#18E3A4"/>
                    <circle cx="65" cy="40" r="3" fill="#18E3A4"/>
                    <circle cx="50" cy="60" r="4" fill="#18E3A4"/>
                    <circle cx="50" cy="70" r="3" fill="#18E3A4"/>
                </svg>
                Midgar
            </div>
            
            <nav>
                <ul>
                    <li><a href="{{ path('home') }}" class="magic-btn">Accueil</a></li>
                    <li><a href="/discover" class="magic-btn">Découvrir</a></li>
                    <li><a href="{{ path('universe_index') }}" class="magic-btn">Univers</a></li>
                    <li><a href="{{ path('personnage_index') }}" class="magic-btn">Personnages</a></li>
                    <li><a href="{{ path('oeuvre_index') }}" class="magic-btn">Œuvre</a></li>
                    <li><a href="{{ path('artefact_index') }}" class="magic-btn">Artefacts</a></li>
                    <li><a href="/shop" class="magic-btn">Boutique</a></li>
                    <li><a href="/challenges" class="magic-btn">Défis</a></li>
                </ul>
            </nav>
            
            <div class="auth-buttons">
                {% if app.user %}
                    <a href="{{ path('profile') }}" class="profile-link magic-btn">
                        <div class="avatar">
                            {% if app.user.avatar %}
                                <img src="{{ asset('uploads/avatars/' ~ app.user.avatar) }}" alt="{{ app.user.username }}">
                            {% else %}
                                <img src="{{ asset('images/default-avatar.png') }}" alt="{{ app.user.username }}">
                            {% endif %}
                        </div>
                        <span>{{ app.user.username }}</span>
                    </a>
                    <a href="{{ path('app_logout') }}" class="btn btn-secondary btn-sm magic-btn">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-secondary btn-sm magic-btn">Connexion</a>
                    <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm magic-btn">S'inscrire</a>
                {% endif %}
            </div>
        </header>

        <!-- ===== MAIN CONTENT ===== -->
        <main>
            <section class="section">
                <div class="container">
                    <div style="max-width: 700px; margin: 0 auto;">
                        <h1 class="text-center" style="color: var(--primary-color); margin-bottom: 0.5rem; text-align: center;">Quiz de Préférences</h1>
                        <p class="text-center" style="color: var(--text-secondary); font-size: 1.1rem; margin-bottom: 2rem; text-align: center;">
                            Découvrez vos préférences en création fantasy
                        </p>

                        <div class="quiz-container">
                            <div class="quiz-question">
                                <h3>{{ question.question }}</h3>

                                <div class="quiz-options">
                                    {% for reponse in reponses %}
                                        <input type="radio" id="reponse-{{ reponse.id }}" name="reponse" value="{{ reponse.id }}">
                                        <label for="reponse-{{ reponse.id }}" class="quiz-option">
                                            {{ reponse.option }}
                                        </label>
                                    {% endfor %}
                                </div>
                            </div>

                            <div class="progress-bar">
                                <div class="progress-fill"></div>
                            </div>
                            <div class="progress-text">
                                Question {{ step }} de {{ total }}
                            </div>

                            <!-- NAVIGATION -->
                            <div style="display: flex; gap: 1rem; margin-top: 2rem; justify-content: space-between;">
                                {% if step > 1 %}
                                    <a href="{{ path('quiz', {step: step - 1}) }}" class="btn btn-secondary magic-btn">Précédent</a>
                                {% else %}
                                    <button class="btn btn-secondary" disabled style="opacity: 0.5; cursor: not-allowed;">Précédent</button>
                                {% endif %}
                                
                                {% if step < total %}
                                    <a href="{{ path('quiz', {step: step + 1}) }}" class="btn btn-primary magic-btn">Suivant</a>
                                {% else %}
                                    <a href="{{ path('quiz', {step: 1}) }}" class="btn btn-primary magic-btn">Terminer</a>
                                {% endif %}
                            </div>

                            <!-- PERSONALIZE FEED -->
                            <div class="personalize-link">
                                ou <a href="/preferences/advanced">personnaliser ton feed</a>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </main>

        <!-- ===== FOOTER ===== -->
        <footer>
            <div class="container">
                <div class="footer-bottom">
                    <p>&copy; 2026 Midgar Fantasy Platform. Créez, partagez, vivez la magie.</p>
                </div>
            </div>
        </footer>
    </div>

    <script>
        // ===== MAGIC BUTTON PARTICLE EFFECT =====
        document.querySelectorAll('.magic-btn, .btn-primary, .btn-secondary').forEach(btn => {
            btn.addEventListener('click', function(e) {
                for(let i = 0; i < 8; i++) {
                    const particle = document.createElement('span');
                    particle.classList.add('particle');
                    const x = (Math.random() - 0.5) * 80 + 'px';
                    const y = (Math.random() - 0.5) * 80 + 'px';
                    particle.style.setProperty('--x', x);
                    particle.style.setProperty('--y', y);
                    
                    const rect = btn.getBoundingClientRect();
                    particle.style.left = (e.clientX - rect.left) + 'px';
                    particle.style.top = (e.clientY - rect.top) + 'px';
                    
                    btn.appendChild(particle);
                    setTimeout(() => particle.remove(), 600);
                }
            });
        });

        // ===== BACKGROUND INITIALIZATION =====
        document.addEventListener('DOMContentLoaded', function() {
            const bgVideo = document.getElementById('bg-video');
            const gradientBg = document.getElementById('gradient-bg');
            
            if (bgVideo) {
                bgVideo.addEventListener('error', function() {
                    console.log('Background video failed to load, using gradient fallback');
                    if (gradientBg) gradientBg.style.display = 'block';
                });
                
                bgVideo.play().catch(function(error) {
                    console.log('Video autoplay prevented:', error);
                    if (gradientBg) gradientBg.style.display = 'block';
                });
            }
            
            document.addEventListener('visibilitychange', function() {
                if (bgVideo) {
                    if (document.hidden) {
                        bgVideo.pause();
                    } else {
                        bgVideo.play().catch(e => console.log('Video play error:', e));
                    }
                }
            });
        });

        // ===== HIGHLIGHT ACTIVE NAV LINK =====
        const currentPath = window.location.pathname;
        document.querySelectorAll('nav .magic-btn').forEach(link => {
            if (link.getAttribute('href') === currentPath || 
                (link.getAttribute('href') === '{{ path('home') }}' && currentPath === '/')) {
                link.classList.add('active');
            }
        });
    </script>
</body>
</html>
>>>>>>> validation-final-2
