<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tous les Univers - Midgar</title>
<<<<<<< HEAD
    <link rel="stylesheet" href="/css/midgar.css">
    <style>
=======
    
    <style>
        /* ===== VARIABLES & RESET ===== */
        :root {
            --primary-color: #18E3A4;
            --primary-dark: #13c891;
            --text-color: #E6FFF6;
            --text-secondary: #B0B9B6;
            --bg-primary: #1A1F1E;
            --bg-dark: #0D0F0F;
            --card-bg: rgba(26, 31, 30, 0.7);
            --glass-bg: rgba(26, 31, 30, 0.5);
            --border-radius: 12px;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }
        
        body {
            color: var(--text-color);
            line-height: 1.6;
            overflow-x: hidden;
        }
        
        a {
            text-decoration: none;
            color: inherit;
        }
        
        ul {
            list-style: none;
        }
        
        .container {
            width: 90%;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        /* ===== BOUTONS ANIMÉS ===== */
        .btn {
            display: inline-block;
            padding: 0.6rem 1.2rem;
            border-radius: var(--border-radius);
            font-weight: 600;
            transition: all 0.3s ease;
            text-align: center;
            border: none;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }
        
        .btn-primary {
            background-color: var(--primary-color);
            color: #000;
        }
        
        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: scale(1.05);
            box-shadow: 0 0 15px var(--primary-color);
        }
        
        .btn-secondary {
            background-color: transparent;
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
        }
        
        .btn-secondary:hover {
            background-color: var(--primary-color);
            color: #000;
            transform: scale(1.05);
            box-shadow: 0 0 15px var(--primary-color);
        }
        
        .btn-sm {
            padding: 0.4rem 0.8rem;
            font-size: 0.9rem;
        }
        
        /* ===== BOUTONS MAGIQUES (magic-btn) ===== */
        .magic-btn {
            position: relative;
            overflow: hidden;
            display: inline-block;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
            padding: 0.5rem 1rem;
            background: transparent;
            color: var(--text-color);
            border: 1px solid transparent;
        }
        
        .magic-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 0 15px var(--primary-color);
            border-color: var(--primary-color);
            color: var(--primary-color);
            background: rgba(24, 227, 164, 0.1);
        }
        
        .magic-btn.active {
            color: var(--primary-color);
            border-color: var(--primary-color);
            background: rgba(24, 227, 164, 0.1);
        }
        
        /* ===== ANIMATION PARTICULES ===== */
        .particle {
            position: absolute;
            width: 6px;
            height: 6px;
            background: radial-gradient(circle, var(--primary-color), var(--primary-dark));
            border-radius: 50%;
            pointer-events: none;
            animation: fly 0.6s forwards;
            opacity: 0.8;
            z-index: 1;
        }
        
        @keyframes fly {
            0% { 
                transform: translate(0, 0) scale(1); 
                opacity: 1; 
            }
            100% { 
                transform: translate(var(--x), var(--y)) scale(0); 
                opacity: 0; 
            }
        }
        
        /* ===== VIDEO BACKGROUND ===== */
        .video-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -2;
            pointer-events: none;
        }
        
        .video-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            z-index: -1;
        }
        
        .gradient-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--bg-primary) 0%, var(--bg-dark) 100%);
            z-index: -3;
            pointer-events: none;
            display: none;
        }
        
        /* ===== HEADER STANDARD ===== */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 100;
            background: rgba(26, 31, 30, 0.9);
            backdrop-filter: blur(10px);
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--primary-color);
            cursor: pointer;
        }
        
        .logo:hover {
            animation: glow 1.5s infinite;
        }
        
        .logo svg {
            width: 32px;
            height: 32px;
        }
        
        @keyframes glow {
            0%, 100% {
                text-shadow: 0 0 5px var(--primary-color);
            }
            50% {
                text-shadow: 0 0 15px var(--primary-color), 0 0 30px var(--primary-color);
            }
        }
        
        nav ul {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }
        
        .auth-buttons {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .profile-link {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 0.3rem 0.6rem;
            transition: all 0.3s ease;
            border-radius: 8px;
        }
        
        .profile-link:hover {
            background: rgba(24, 227, 164, 0.1);
        }
        
        .avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            overflow: hidden;
            border: 2px solid var(--primary-color);
            transition: transform 0.3s;
        }
        
        .avatar:hover {
            transform: scale(1.1);
            box-shadow: 0 0 10px var(--primary-color);
        }
        
        .avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        /* ===== SITE CONTENT ===== */
        .site-content {
            position: relative;
            z-index: 1;
            min-height: 100vh;
        }
        
        main {
            padding-top: 80px;
            min-height: 100vh;
        }
        
        .section {
            padding: 4rem 0;
            background-color: var(--glass-bg);
            border-radius: 16px;
            margin: 2rem auto;
            backdrop-filter: blur(10px);
            max-width: 1200px;
            border: 1px solid rgba(24, 227, 164, 0.1);
            transition: border-color 0.3s;
        }
        
        .section:hover {
            border-color: rgba(24, 227, 164, 0.3);
        }
        
        /* ===== STYLES SPÉCIFIQUES UNIVERS ===== */
>>>>>>> afea128 (generation des avatars)
        .universes-container {
            display: grid;
            grid-template-columns: 250px 1fr;
            gap: 2rem;
            margin-top: 2rem;
        }

        .filter-sidebar {
<<<<<<< HEAD
            background-color: #1A1F1E;
            padding: 1.5rem;
            border-radius: 6px;
            border: 1px solid #2A3139;
            height: fit-content;
            position: sticky;
            top: 100px;
=======
            background-color: var(--card-bg);
            padding: 1.5rem;
            border-radius: var(--border-radius);
            border: 1px solid rgba(24, 227, 164, 0.1);
            height: fit-content;
            position: sticky;
            top: 100px;
            backdrop-filter: blur(5px);
        }

        .filter-sidebar:hover {
            border-color: rgba(24, 227, 164, 0.3);
>>>>>>> afea128 (generation des avatars)
        }

        .filter-group {
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
<<<<<<< HEAD
            border-bottom: 1px solid #2A3139;
=======
            border-bottom: 1px solid rgba(24, 227, 164, 0.1);
>>>>>>> afea128 (generation des avatars)
        }

        .filter-group:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }

        .filter-group h4 {
<<<<<<< HEAD
            color: #18E3A4;
=======
            color: var(--primary-color);
>>>>>>> afea128 (generation des avatars)
            font-size: 0.9rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 1rem;
            margin-top: 0;
        }

        .filter-option {
            display: flex;
            gap: 0.75rem;
            margin-bottom: 0.75rem;
            cursor: pointer;
            align-items: center;
        }

        .filter-option input[type="checkbox"],
        .filter-option input[type="radio"] {
            cursor: pointer;
<<<<<<< HEAD
            accent-color: #18E3A4;
=======
            accent-color: var(--primary-color);
>>>>>>> afea128 (generation des avatars)
            width: 18px;
            height: 18px;
        }

        .filter-option label {
            cursor: pointer;
<<<<<<< HEAD
            color: #E6FFF6;
=======
            color: var(--text-color);
>>>>>>> afea128 (generation des avatars)
            font-size: 0.9rem;
            margin: 0;
            flex: 1;
        }

        .search-box {
            width: 100%;
            padding: 10px;
<<<<<<< HEAD
            background-color: #0B0F0E;
            border: 1px solid #2A3139;
            border-radius: 4px;
            color: #E6FFF6;
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }

        .search-box::placeholder { color: #B0B9B6; }
        .search-box:focus {
            outline: none;
            border-color: #18E3A4;
            box-shadow: 0 0 10px rgba(24, 227, 164, 0.2);
=======
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(24, 227, 164, 0.3);
            border-radius: 6px;
            color: var(--text-color);
            font-size: 0.9rem;
            margin-bottom: 1rem;
            transition: all 0.3s;
        }

        .search-box::placeholder { 
            color: var(--text-secondary); 
        }
        
        .search-box:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 10px rgba(24, 227, 164, 0.3);
>>>>>>> afea128 (generation des avatars)
        }

        .filter-reset {
            width: 100%;
            padding: 10px;
<<<<<<< HEAD
            background-color: #2A3139;
            color: #18E3A4;
            border: 1px solid #18E3A4;
            border-radius: 4px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
        }

        .filter-reset:hover {
            background-color: #18E3A4;
            color: #0B0F0E;
        }

        .universes-section { flex: 1; }
=======
            background: transparent;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
            border-radius: 6px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
            display: block;
            text-align: center;
            text-decoration: none;
            margin-top: 1rem;
        }

        .filter-reset:hover {
            background-color: var(--primary-color);
            color: #000;
            transform: scale(1.02);
            box-shadow: 0 0 15px var(--primary-color);
        }

        .universes-section { 
            flex: 1; 
        }
>>>>>>> afea128 (generation des avatars)

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            gap: 1rem;
        }

<<<<<<< HEAD
        .section-title { margin: 0; }

        .create-cta {
            padding: 12px 24px;
            background-color: #18E3A4;
            color: #0B0F0E;
            border: none;
            border-radius: 4px;
=======
        .section-title { 
            margin: 0; 
        }

        .create-cta {
            padding: 12px 24px;
            background-color: var(--primary-color);
            color: #000;
            border: none;
            border-radius: var(--border-radius);
>>>>>>> afea128 (generation des avatars)
            font-weight: 700;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s;
        }

<<<<<<< HEAD
        .create-cta:hover { background-color: #0DCF92; }
=======
        .create-cta:hover { 
            background-color: var(--primary-dark);
            transform: scale(1.05);
            box-shadow: 0 0 15px var(--primary-color);
        }
>>>>>>> afea128 (generation des avatars)

        .universes-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .universe-card {
<<<<<<< HEAD
            background-color: #1A1F1E;
            border-radius: 8px;
            overflow: hidden;
            border: 1px solid #2A3139;
            transition: all 0.3s;
            cursor: pointer;
            display: flex;
            flex-direction: column;
        }

        .universe-card:hover {
            border-color: #18E3A4;
            box-shadow: 0 0 20px rgba(24, 227, 164, 0.15);
            transform: translateY(-2px);
=======
            background-color: var(--card-bg);
            border-radius: var(--border-radius);
            overflow: hidden;
            border: 1px solid transparent;
            transition: all 0.3s ease;
            cursor: pointer;
            display: flex;
            flex-direction: column;
            backdrop-filter: blur(5px);
        }

        .universe-card:hover {
            border-color: rgba(24, 227, 164, 0.2);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.6), 0 0 20px rgba(24, 227, 164, 0.3);
            transform: translateY(-10px) scale(1.02);
>>>>>>> afea128 (generation des avatars)
        }

        .card-banner {
            width: 100%;
            height: 160px;
<<<<<<< HEAD
            background: linear-gradient(135deg, #18E3A4, #0DCF92);
=======
            background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
>>>>>>> afea128 (generation des avatars)
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            overflow: hidden;
        }
<<<<<<< HEAD
=======
        
        .card-banner img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
            transition: transform 0.5s ease;
        }
        
        .universe-card:hover .card-banner img {
            transform: scale(1.1);
        }
>>>>>>> afea128 (generation des avatars)

        .card-content {
            padding: 1.5rem;
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .card-title {
            font-size: 1.2rem;
            font-weight: 700;
<<<<<<< HEAD
            color: #E6FFF6;
=======
            color: var(--primary-color);
>>>>>>> afea128 (generation des avatars)
            margin: 0 0 0.5rem 0;
            line-height: 1.3;
        }

        .card-genre-tags {
            display: flex;
            gap: 0.5rem;
            margin-bottom: 0.75rem;
            flex-wrap: wrap;
        }

        .genre-tag {
            display: inline-block;
            padding: 4px 10px;
<<<<<<< HEAD
            background-color: #0B0F0E;
            border: 1px solid #18E3A4;
            color: #18E3A4;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 600;
=======
            background-color: rgba(24, 227, 164, 0.1);
            border: 1px solid var(--primary-color);
            color: var(--primary-color);
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 600;
            transition: transform 0.2s;
        }
        
        .genre-tag:hover {
            transform: scale(1.05);
>>>>>>> afea128 (generation des avatars)
        }

        .card-description {
            font-size: 0.85rem;
<<<<<<< HEAD
            color: #B0B9B6;
=======
            color: var(--text-secondary);
>>>>>>> afea128 (generation des avatars)
            margin-bottom: 1rem;
            flex: 1;
            line-height: 1.5;
        }

        .card-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.85rem;
<<<<<<< HEAD
            color: #B0B9B6;
            padding-top: 1rem;
            border-top: 1px solid #2A3139;
=======
            color: var(--text-secondary);
            padding-top: 1rem;
            border-top: 1px solid rgba(24, 227, 164, 0.1);
>>>>>>> afea128 (generation des avatars)
        }

        .card-stats {
            display: flex;
            gap: 1rem;
        }

        .stat {
            display: flex;
            align-items: center;
            gap: 0.3rem;
        }

        .creator-info {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .creator-avatar {
            width: 24px;
            height: 24px;
            border-radius: 50%;
<<<<<<< HEAD
            background: linear-gradient(135deg, #18E3A4, #0DCF92);
=======
            background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
>>>>>>> afea128 (generation des avatars)
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.8rem;
<<<<<<< HEAD
        }

        .results-count { color: #B0B9B6; font-size: 0.9rem; }

        .load-more { display: flex; justify-content: center; margin-top: 2rem; }

        .load-more-btn {
            padding: 12px 40px;
            background-color: #18E3A4;
            color: #0B0F0E;
            border: none;
            border-radius: 4px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s;
        }

        .load-more-btn:hover { background-color: #0DCF92; }
=======
            color: #000;
            font-weight: 700;
        }

        .results-count { 
            color: var(--text-secondary); 
            font-size: 0.9rem; 
        }
>>>>>>> afea128 (generation des avatars)

        .empty-state {
            text-align: center;
            padding: 3rem;
<<<<<<< HEAD
            color: #B0B9B6;
        }

        .empty-state p { font-size: 1.1rem; margin-bottom: 1rem; }

        @media (max-width: 1024px) {
            .universes-container { grid-template-columns: 1fr; }
            .filter-sidebar { position: static; }
        }

        @media (max-width: 768px) {
            .universes-grid { grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); }
=======
            color: var(--text-secondary);
        }

        .empty-state p { 
            font-size: 1.1rem; 
            margin-bottom: 1rem; 
        }

        .flash-success {
            background-color: rgba(24, 227, 164, 0.2);
            color: var(--primary-color);
            padding: 1rem;
            border-radius: var(--border-radius);
            margin-bottom: 1rem;
            font-weight: 600;
            border: 1px solid var(--primary-color);
        }
        
        /* ===== MODALES STANDARD ===== */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            z-index: 1000;
            justify-content: center;
            align-items: center;
            animation: fadeIn 0.3s;
            backdrop-filter: blur(10px);
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        .modal-content {
            background: var(--bg-primary);
            padding: 2rem;
            border-radius: var(--border-radius);
            max-width: 800px;
            width: 90%;
            max-height: 90vh;
            overflow-y: auto;
            position: relative;
            border: 1px solid var(--primary-color);
            animation: slideUp 0.4s;
            box-shadow: 0 0 50px rgba(24, 227, 164, 0.3);
        }
        
        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .modal-close {
            position: absolute;
            top: 1rem;
            right: 1rem;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--primary-color);
            background: none;
            border: none;
            padding: 0.5rem;
            transition: all 0.3s;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .modal-close:hover {
            background: var(--primary-color);
            color: #000;
            transform: rotate(90deg);
        }
        
        .modal-image-container {
            width: 100%;
            border-radius: var(--border-radius);
            overflow: hidden;
            margin-bottom: 1.5rem;
        }
        
        .modal-image-container img {
            width: 100%;
            height: auto;
            max-height: 500px;
            object-fit: contain;
            display: block;
        }
        
        .modal-title {
            color: var(--primary-color);
            margin-bottom: 1rem;
            font-size: 1.8rem;
        }
        
        .modal-body {
            color: var(--text-secondary);
            line-height: 1.8;
        }
        
        /* ===== FOOTER ===== */
        footer {
            background-color: rgba(13, 15, 15, 0.5);
            padding: 3rem 0;
            margin-top: 4rem;
            border-radius: 16px;
            backdrop-filter: blur(8px);
            text-align: center;
            border-top: 1px solid rgba(24, 227, 164, 0.1);
        }
        
        footer:hover {
            border-top-color: rgba(24, 227, 164, 0.3);
        }
        
        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2rem;
            margin-bottom: 2rem;
            text-align: left;
        }
        
        .footer-section h4 {
            color: var(--primary-color);
            margin-bottom: 1rem;
        }
        
        .footer-links {
            list-style: none;
        }
        
        .footer-links li {
            margin-bottom: 0.5rem;
        }
        
        .footer-links a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: color 0.3s;
        }
        
        .footer-links a:hover {
            color: var(--primary-color);
        }
        
        .footer-bottom {
            text-align: center;
            color: var(--text-secondary);
            padding-top: 2rem;
            border-top: 1px solid rgba(24, 227, 164, 0.1);
        }
        
        /* ===== RESPONSIVE ===== */
        @media (max-width: 1024px) {
            .universes-container { 
                grid-template-columns: 1fr; 
            }
            .filter-sidebar { 
                position: static; 
            }
        }

        @media (max-width: 768px) {
            header {
                flex-direction: column;
                padding: 1rem;
                gap: 1rem;
            }
            
            nav ul {
                justify-content: center;
            }
            
            .auth-buttons {
                justify-content: center;
            }
            
            main {
                padding-top: 120px;
            }
            
            .section {
                margin: 1rem;
                padding: 2rem 0;
            }
            
            .universes-grid { 
                grid-template-columns: 1fr; 
            }
            
            .modal-content {
                padding: 1.5rem;
            }
>>>>>>> afea128 (generation des avatars)
        }
    </style>
</head>
<body>
<<<<<<< HEAD
    <header>
        <div class="logo">
            <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
                <path d="M20,80 Q20,30 50,20 Q80,30 80,80 Z" fill="none" stroke="#18E3A4" stroke-width="2"/>
                <line x1="50" y1="20" x2="50" y2="50" stroke="#18E3A4" stroke-width="1.5"/>
                <circle cx="35" cy="40" r="3" fill="#18E3A4"/>
                <circle cx="65" cy="40" r="3" fill="#18E3A4"/>
                <circle cx="50" cy="60" r="4" fill="#18E3A4"/>
                <circle cx="50" cy="70" r="3" fill="#18E3A4"/>
            </svg>
            Midgar
        </div>
        <nav>
            <ul>
                <li><a href="/">Accueil</a></li>
                <li><a href="/discover">Découvrir</a></li>
                <li><a href="/universes">Univers</a></li>
                <li><a href="/oeuvre">Œuvre</a></li>
                <li><a href="/shop">Boutique</a></li>
                <li><a href="/challenges">Défis</a></li>
            </ul>
        </nav>
        <div class="auth-buttons">
            <a href="/login" class="btn btn-secondary btn-sm">Connexion</a>
            <a href="/register" class="btn btn-primary btn-sm">S'inscrire</a>
        </div>
    </header>

    <main>
        <section class="section">
            <div class="container">
                <div class="section-header">
                    <div>
                        <h1 style="margin-bottom: 0.5rem;">Tous les Univers</h1>
                        <p style="color: #B0B9B6; margin: 0;">Explorez les mondes créés par notre communauté</p>
                    </div>
                    <div class="create-ctas">
                        <a href="/create-universe" class="create-cta">+ Créer un Univers</a>
                        <a href="/create-personnage" class="create-cta" style="background-color: transparent; border: 1px solid #18E3A4; color: #18E3A4;">+ Créer un Personnage</a>
                    </div>
                </div>

                <div class="universes-container">
                    <div class="filter-sidebar">
                        <div class="filter-group">
                            <input type="text" class="search-box" id="searchInput" placeholder="Chercher...">
                        </div>

                        <div class="filter-group">
                            <h4>Genre</h4>
                            <div class="filter-option">
                                <input type="checkbox" id="genre-high-fantasy" value="high-fantasy" class="genre-filter">
                                <label for="genre-high-fantasy">Haute Fantaisie</label>
                            </div>
                            <div class="filter-option">
                                <input type="checkbox" id="genre-dark-fantasy" value="dark-fantasy" class="genre-filter">
                                <label for="genre-dark-fantasy">Sombre</label>
                            </div>
                            <div class="filter-option">
                                <input type="checkbox" id="genre-scifi-fantasy" value="scifi-fantasy" class="genre-filter">
                                <label for="genre-scifi-fantasy">Sci-Fi</label>
                            </div>
                            <div class="filter-option">
                                <input type="checkbox" id="genre-urban-fantasy" value="urban-fantasy" class="genre-filter">
                                <label for="genre-urban-fantasy">Urban</label>
                            </div>
                        </div>

                        <div class="filter-group">
                            <h4>Thèmes</h4>
                            <div class="filter-option">
                                <input type="checkbox" id="theme-magie" value="magie" class="theme-filter">
                                <label for="theme-magie">Magie</label>
                            </div>
                            <div class="filter-option">
                                <input type="checkbox" id="theme-guerre" value="guerre" class="theme-filter">
                                <label for="theme-guerre">Guerre</label>
                            </div>
                            <div class="filter-option">
                                <input type="checkbox" id="theme-politique" value="politique" class="theme-filter">
                                <label for="theme-politique">Politique</label>
                            </div>
                            <div class="filter-option">
                                <input type="checkbox" id="theme-nature" value="nature" class="theme-filter">
                                <label for="theme-nature">Nature</label>
                            </div>
                            <div class="filter-option">
                                <input type="checkbox" id="theme-mystere" value="mystere" class="theme-filter">
                                <label for="theme-mystere">Mystère</label>
                            </div>
                            <div class="filter-option">
                                <input type="checkbox" id="theme-aventure" value="aventure" class="theme-filter">
                                <label for="theme-aventure">Aventure</label>
                            </div>
                        </div>

                        <div class="filter-group">
                            <h4>Tri</h4>
                            <div class="filter-option">
                                <input type="radio" id="sort-newest" name="sort" value="newest" class="sort-filter" checked>
                                <label for="sort-newest">Récent</label>
                            </div>
                            <div class="filter-option">
                                <input type="radio" id="sort-popular" name="sort" value="popular" class="sort-filter">
                                <label for="sort-popular">Populaire</label>
                            </div>
                            <div class="filter-option">
                                <input type="radio" id="sort-trending" name="sort" value="trending" class="sort-filter">
                                <label for="sort-trending">Tendance</label>
                            </div>
                        </div>

                        <button class="filter-reset" id="resetFilters">Réinitialiser</button>
                    </div>

                    <div class="universes-section">
                        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; padding-bottom: 1rem; border-bottom: 1px solid #2A3139;">
                            <h2 style="margin: 0; color: #E6FFF6;">Univers</h2>
                            <div class="results-count">
                                <span id="displayedCount">0</span> / <span id="totalCount">0</span>
                            </div>
                        </div>

                        <div class="universes-grid" id="universesGrid"></div>

                        <div class="load-more" id="loadMoreContainer" style="display: none;">
                            <button class="load-more-btn" id="loadMoreBtn">Charger Plus</button>
                        </div>

                        <div class="empty-state" id="emptyState" style="display: none;">
                            <p>😔 Aucun univers ne correspond à vos critères</p>
                        </div>
                    </div>
                </div>

            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <div class="footer-grid">
                <div class="footer-section">
                    <h4>À propos</h4>
                    <ul class="footer-links">
                        <li><a href="#">À propos de Midgar</a></li>
                        <li><a href="#">Notre mission</a></li>
                        <li><a href="#">Équipe</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h4>Ressources</h4>
                    <ul class="footer-links">
                        <li><a href="#">Aide & Support</a></li>
                        <li><a href="#">Documentation</a></li>
                        <li><a href="#">Blog</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h4>Légal</h4>
                    <ul class="footer-links">
                        <li><a href="#">Conditions d'utilisation</a></li>
                        <li><a href="#">Politique de confidentialité</a></li>
                        <li><a href="#">Cookies</a></li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2026 Midgar Fantasy Platform. Créez, partagez, vivez la magie.</p>
            </div>
        </div>
    </footer>

    <script>
        const mockUniverses = [
            {id:1,name:"Nuit Éternelle",genre:["dark-fantasy"],themes:["mystere","magie"],desc:"Monde plongé dans une nuit perpétuelle",banner:"🌙",creator:"Léa M.",avatar:"L",personnages:12,oeuvres:28,artefacts:7,likes:1240,date:new Date(2026,0,20)},
            {id:2,name:"Châteaux Flottants",genre:["high-fantasy"],themes:["aventure","magie"],desc:"Forteresses majestueuses flottent dans les nuages",banner:"☁️",creator:"Léa M.",avatar:"L",personnages:18,oeuvres:45,artefacts:12,likes:892,date:new Date(2026,0,18)},
            {id:3,name:"Dragons de Lumière",genre:["high-fantasy"],themes:["aventure","nature"],desc:"Univers dominé par les dragons",banner:"🐲",creator:"Alexis S.",avatar:"A",personnages:14,oeuvres:32,artefacts:9,likes:654,date:new Date(2026,0,15)},
            {id:4,name:"Techno-Mystique",genre:["scifi-fantasy"],themes:["magie","guerre"],desc:"Technologie avancée rencontre magie ancienne",banner:"⚡",creator:"Marcus T.",avatar:"M",personnages:22,oeuvres:51,artefacts:15,likes:1120,date:new Date(2026,0,25)},
            {id:5,name:"Forêt Éternelle",genre:["high-fantasy"],themes:["nature","magie"],desc:"Forêt ancienne où les arbres parlent",banner:"🌲",creator:"Elena R.",avatar:"E",personnages:16,oeuvres:37,artefacts:8,likes:756,date:new Date(2026,0,22)},
            {id:6,name:"Royaumes Souterrains",genre:["dark-fantasy","high-fantasy"],themes:["guerre","politique"],desc:"Empires dwarves et gnomes sous les montagnes",banner:"⛏️",creator:"Thor N.",avatar:"T",personnages:20,oeuvres:48,artefacts:14,likes:892,date:new Date(2026,0,19)},
            {id:7,name:"Cités Flottantes",genre:["scifi-fantasy"],themes:["aventure","politique"],desc:"Villes futuristes flottant au-dessus d'un monde post-apocalyptique",banner:"🏙️",creator:"Luna K.",avatar:"L",personnages:19,oeuvres:42,artefacts:11,likes:978,date:new Date(2026,0,17)},
            {id:8,name:"Océan Enchanté",genre:["high-fantasy","urban-fantasy"],themes:["magie","mystere"],desc:"Royaumes sous-marins remplis de créatures magiques",banner:"🌊",creator:"Nerida M.",avatar:"N",personnages:13,oeuvres:29,artefacts:10,likes:645,date:new Date(2026,0,16)},
            {id:9,name:"Domaine des Ombres",genre:["dark-fantasy"],themes:["guerre","mystere"],desc:"Où les ombres ont pris vie",banner:"🌑",creator:"Victor D.",avatar:"V",personnages:17,oeuvres:40,artefacts:13,likes:834,date:new Date(2026,0,14)},
            {id:10,name:"Empire Cristallin",genre:["high-fantasy"],themes:["magie","aventure"],desc:"Empire construit entièrement de cristaux magiques",banner:"💎",creator:"Sylvia P.",avatar:"S",personnages:15,oeuvres:35,artefacts:9,likes:723,date:new Date(2026,0,21)},
            {id:11,name:"Steppes Sauvages",genre:["high-fantasy"],themes:["nature","aventure"],desc:"Prairies infinies peuplées de tribus guerrières",banner:"🏜️",creator:"Khan B.",avatar:"K",personnages:21,oeuvres:44,artefacts:8,likes:567,date:new Date(2026,0,13)},
            {id:12,name:"Métropole Gothique",genre:["urban-fantasy"],themes:["mystere","politique"],desc:"Grande ville moderne cachant secrets magiques",banner:"🏛️",creator:"Isabelle V.",avatar:"I",personnages:18,oeuvres:39,artefacts:7,likes:891,date:new Date(2026,0,12)}
        ];

        let filtered = [...mockUniverses];
        let displayedItems = 12;

        document.addEventListener('DOMContentLoaded', () => {
            renderUniverses();
            attachListeners();
        });

        function attachListeners(){
            document.querySelectorAll('.genre-filter').forEach(cb => cb.addEventListener('change', filterUniverses));
            document.querySelectorAll('.theme-filter').forEach(cb => cb.addEventListener('change', filterUniverses));
            document.querySelectorAll('.sort-filter').forEach(r => r.addEventListener('change', filterUniverses));
            document.getElementById('searchInput').addEventListener('input', filterUniverses);
            document.getElementById('resetFilters').addEventListener('click', () => {
                document.querySelectorAll('input[type="checkbox"]').forEach(cb => cb.checked = false);
                document.getElementById('sort-newest').checked = true;
                document.getElementById('searchInput').value = '';
                filtered = [...mockUniverses];
                displayedItems = 12;
                renderUniverses();
            });
            document.getElementById('loadMoreBtn')?.addEventListener('click', () => { displayedItems += 12; renderUniverses(); });
        }

        function filterUniverses(){
            const genres = Array.from(document.querySelectorAll('.genre-filter:checked')).map(cb => cb.value);
            const themes = Array.from(document.querySelectorAll('.theme-filter:checked')).map(cb => cb.value);
            const search = document.getElementById('searchInput').value.toLowerCase();
            const sort = document.querySelector('.sort-filter:checked').value;

            filtered = mockUniverses.filter(item => {
                if(genres.length > 0 && !genres.some(g => item.genre.includes(g))) return false;
                if(themes.length > 0 && !themes.some(t => item.themes.includes(t))) return false;
                if(search && !item.name.toLowerCase().includes(search)) return false;
                return true;
            });

            if(sort === 'newest') filtered.sort((a,b) => b.date - a.date);
            else if(sort === 'popular') filtered.sort((a,b) => (b.likes || b.personnages) - (a.likes || a.personnages));
            else filtered.sort((a,b) => (b.likes || 0) - (a.likes || 0));

            displayedItems = 12;
            renderUniverses();
        }

        function renderUniverses(){
            const grid = document.getElementById('universesGrid');
            const empty = document.getElementById('emptyState');
            const more = document.getElementById('loadMoreContainer');

            grid.innerHTML = '';
            if(filtered.length === 0) { empty.style.display = 'block'; more.style.display = 'none'; }
            else {
                empty.style.display = 'none';
                filtered.slice(0, displayedItems).forEach(item => {
                    const card = document.createElement('div');
                    card.className = 'universe-card';
                    const genreMap = {'high-fantasy':'Haute Fantaisie','dark-fantasy':'Sombre','scifi-fantasy':'Sci-Fi','urban-fantasy':'Urban'};
                    const genreTags = item.genre.map(g => `<span class="genre-tag">${genreMap[g] || g}</span>`).join('');
                    card.innerHTML = `
                        <div class="card-banner">${item.banner}</div>
                        <div class="card-content">
                            <h3 class="card-title">${item.name}</h3>
                            <div class="card-genre-tags">${genreTags}</div>
                            <p class="card-description">${item.desc}</p>
                            <div class="card-footer">
                                <div class="card-stats"><div class="stat">Characters ${item.personnages}</div><div class="stat">Works ${item.oeuvres}</div><div class="stat">Artifacts ${item.artefacts}</div></div>
                                <div class="creator-info"><div class="creator-avatar">${item.avatar}</div><span>${item.creator}</span></div>
                            </div>
                        </div>
                    `;
                    card.addEventListener('click', () => window.location.href = `/universe-detail?id=${item.id}`);
                    grid.appendChild(card);
                });
                more.style.display = filtered.length > displayedItems ? 'flex' : 'none';
            }
            document.getElementById('displayedCount').textContent = Math.min(displayedItems, filtered.length);
            document.getElementById('totalCount').textContent = filtered.length;
        }
=======
    <!-- ===== BACKGROUND STANDARD ===== -->
    <div class="gradient-background" id="gradient-bg"></div>
    <div class="video-overlay"></div>
    <video id="bg-video" class="video-background" autoplay muted loop playsinline>
        <source src="{{ asset('uploads/dragon.mp4') }}" type="video/mp4">
        Votre navigateur ne supporte pas la vidéo HTML5.
    </video>

    <!-- ===== SITE CONTENT ===== -->
    <div class="site-content">
        <!-- ===== HEADER STANDARD AVEC MAGIC-BTN ===== -->
        <header>
            <div class="logo">
                <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
                    <path d="M20,80 Q20,30 50,20 Q80,30 80,80 Z" fill="none" stroke="#18E3A4" stroke-width="2"/>
                    <line x1="50" y1="20" x2="50" y2="50" stroke="#18E3A4" stroke-width="1.5"/>
                    <circle cx="35" cy="40" r="3" fill="#18E3A4"/>
                    <circle cx="65" cy="40" r="3" fill="#18E3A4"/>
                    <circle cx="50" cy="60" r="4" fill="#18E3A4"/>
                    <circle cx="50" cy="70" r="3" fill="#18E3A4"/>
                </svg>
                Midgar
            </div>
            
            <nav>
                <ul>
                    <li><a href="{{ path('home') }}" class="magic-btn">Accueil</a></li>
                    <li><a href="/discover" class="magic-btn">Découvrir</a></li>
                    <li><a href="{{ path('universe_index') }}" class="magic-btn active">Univers</a></li>
                    <li><a href="/personnages" class="magic-btn">Personnages</a></li>
                    <li><a href="{{ path('oeuvre_index') }}" class="magic-btn">Œuvre</a></li>
                    <li><a href="{{ path('artefact_index') }}" class="magic-btn">Artefacts</a></li>
                    <li><a href="/shop" class="magic-btn">Boutique</a></li>
                    <li><a href="/challenges" class="magic-btn">Défis</a></li>
                </ul>
            </nav>
            
            <div class="auth-buttons">
                {% if app.user %}
                    <a href="{{ path('profile') }}" class="profile-link magic-btn">
                        <div class="avatar">
                            {% if app.user.avatar %}
                                <img src="{{ asset('uploads/avatars/' ~ app.user.avatar) }}" alt="{{ app.user.username }}">
                            {% else %}
                                <img src="{{ asset('images/default-avatar.png') }}" alt="{{ app.user.username }}">
                            {% endif %}
                        </div>
                        <span>{{ app.user.username }}</span>
                    </a>
                    <a href="{{ path('app_logout') }}" class="btn btn-secondary btn-sm magic-btn">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-secondary btn-sm magic-btn">Connexion</a>
                    <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm magic-btn">S'inscrire</a>
                {% endif %}
            </div>
        </header>

        <!-- ===== MAIN CONTENT ===== -->
        <main>
            <section class="section">
                <div class="container">
                    {% for flash_message in app.flashes('success') %}
                        <div class="flash-success">{{ flash_message }}</div>
                    {% endfor %}

                    <div class="section-header">
                        <div>
                            <h1 style="margin-bottom: 0.5rem; color: var(--primary-color);">Tous les Univers</h1>
                            <p style="color: var(--text-secondary); margin: 0;">Explorez les mondes créés par notre communauté</p>
                        </div>
                        <div class="create-ctas">
                            <a href="{{ path('universe_new') }}" class="create-cta magic-btn">+ Créer un Univers</a>
                        </div>
                    </div>

                    <div class="universes-container">
                        <div class="filter-sidebar">
                            <form method="get" action="{{ path('universe_index') }}">
                                <div class="filter-group">
                                    <input type="text" name="search" class="search-box" placeholder="Chercher..." value="{{ app.request.query.get('search') }}">
                                </div>

                                <div class="filter-group">
                                    <h4>Tri</h4>
                                    <div class="filter-option">
                                        <input type="radio" id="sort-newest" name="sort" value="newest" {% if app.request.query.get('sort') == 'newest' or not app.request.query.get('sort') %}checked{% endif %}>
                                        <label for="sort-newest">Plus récent</label>
                                    </div>
                                    <div class="filter-option">
                                        <input type="radio" id="sort-oldest" name="sort" value="oldest" {% if app.request.query.get('sort') == 'oldest' %}checked{% endif %}>
                                        <label for="sort-oldest">Plus ancien</label>
                                    </div>
                                    <div class="filter-option">
                                        <input type="radio" id="sort-name" name="sort" value="name" {% if app.request.query.get('sort') == 'name' %}checked{% endif %}>
                                        <label for="sort-name">Nom (A-Z)</label>
                                    </div>
                                </div>

                                <button type="submit" class="filter-reset magic-btn">Filtrer</button>
                                <a href="{{ path('universe_index') }}" class="filter-reset magic-btn">Réinitialiser</a>
                            </form>
                        </div>

                        <div class="universes-section">
                            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; padding-bottom: 1rem; border-bottom: 1px solid rgba(24, 227, 164, 0.1);">
                                <div>
                                    <h2 style="margin: 0; color: var(--primary-color);">Univers</h2>
                                    <p style="color: var(--text-secondary); margin: 0; font-size: 0.9rem;">
                                        Affichage: <a href="{{ path('universe_index') }}" style="color: var(--primary-color);">Univers</a> • 
                                        <a href="/personnages" style="color: var(--text-secondary);">Personnages</a>
                                    </p>
                                </div>
                                <div class="results-count">
                                    <span>{{ universes|length }}</span> univers{{ universes|length != 1 ? 's' : '' }}
                                </div>
                            </div>

                            <div class="universes-grid" id="universesGrid">
                                {% for u in universes %}
                                    <div class="universe-card" onclick="location.href='{{ path('universe_show', {'id': u.id}) }}'">
                                        <div class="card-banner">
                                            {% if u.getBannerBase64() %}
                                                <img src="{{ u.getBannerBase64() }}" alt="banner">
                                            {% else %}
                                                🌌
                                            {% endif %}
                                        </div>
                                        <div class="card-content">
                                            <h3 class="card-title">{{ u.name }}</h3>
                                            <div class="card-genre-tags">
                                                <span class="genre-tag">{{ {'high-fantasy':'Haute Fantaisie','dark-fantasy':'Sombre','scifi-fantasy':'Sci-Fi','urban-fantasy':'Urban'}[u.genre] ?? u.genre }}</span>
                                            </div>
                                            <p class="card-description">{{ u.shortDescription }}</p>
                                            <div class="card-footer">
                                                <div class="card-stats">
                                                    <div class="stat">Personnages {{ u.personnages|length }}</div>
                                                </div>
                                                <div class="creator-info">
                                                    <div class="creator-avatar">{{ u.name|first|upper }}</div>
                                                    <span>—</span>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                {% else %}
                                    <div class="empty-state">
                                        <p>😔 Aucun univers trouvé.</p>
                                        <a href="{{ path('universe_new') }}" class="create-cta magic-btn">Créer le premier univers</a>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </main>

        <!-- ===== FOOTER ===== -->
        <footer>
            <div class="container">
                <div class="footer-grid">
                    <div class="footer-section">
                        <h4>À propos</h4>
                        <ul class="footer-links">
                            <li><a href="#">À propos de Midgar</a></li>
                            <li><a href="#">Notre mission</a></li>
                            <li><a href="#">Équipe</a></li>
                        </ul>
                    </div>
                    <div class="footer-section">
                        <h4>Ressources</h4>
                        <ul class="footer-links">
                            <li><a href="#">Aide & Support</a></li>
                            <li><a href="#">Documentation</a></li>
                            <li><a href="#">Blog</a></li>
                        </ul>
                    </div>
                    <div class="footer-section">
                        <h4>Légal</h4>
                        <ul class="footer-links">
                            <li><a href="#">Conditions d'utilisation</a></li>
                            <li><a href="#">Politique de confidentialité</a></li>
                            <li><a href="#">Cookies</a></li>
                        </ul>
                    </div>
                </div>
                <div class="footer-bottom">
                    <p>&copy; 2026 Midgar Fantasy Platform. Créez, partagez, vivez la magie.</p>
                </div>
            </div>
        </footer>
    </div>
    
    <!-- ===== MODALE IMAGE STANDARD ===== -->
    <div id="imageModal" class="modal">
        <div class="modal-content">
            <button class="modal-close" onclick="closeModal('imageModal')">&times;</button>
            <div class="modal-image-container">
                <img id="modalImage" src="" alt="Image agrandie">
            </div>
            <h3 class="modal-title" id="modalImageTitle"></h3>
        </div>
    </div>

    <script>
        // ===== MAGIC BUTTON PARTICLE EFFECT =====
        document.querySelectorAll('.magic-btn, .btn-primary, .btn-secondary, .create-cta, .filter-reset').forEach(btn => {
            btn.addEventListener('click', function(e) {
                for(let i = 0; i < 8; i++) {
                    const particle = document.createElement('span');
                    particle.classList.add('particle');
                    const x = (Math.random() - 0.5) * 80 + 'px';
                    const y = (Math.random() - 0.5) * 80 + 'px';
                    particle.style.setProperty('--x', x);
                    particle.style.setProperty('--y', y);
                    
                    const rect = btn.getBoundingClientRect();
                    particle.style.left = (e.clientX - rect.left) + 'px';
                    particle.style.top = (e.clientY - rect.top) + 'px';
                    
                    btn.appendChild(particle);
                    setTimeout(() => particle.remove(), 600);
                }
            });
        });

        // ===== BACKGROUND INITIALIZATION =====
        document.addEventListener('DOMContentLoaded', function() {
            const bgVideo = document.getElementById('bg-video');
            const gradientBg = document.getElementById('gradient-bg');
            
            if (bgVideo) {
                bgVideo.addEventListener('error', function() {
                    console.log('Background video failed to load, using gradient fallback');
                    if (gradientBg) gradientBg.style.display = 'block';
                });
                
                bgVideo.play().catch(function(error) {
                    console.log('Video autoplay prevented:', error);
                    if (gradientBg) gradientBg.style.display = 'block';
                });
            }
            
            document.addEventListener('visibilitychange', function() {
                if (bgVideo) {
                    if (document.hidden) {
                        bgVideo.pause();
                    } else {
                        bgVideo.play().catch(e => console.log('Video play error:', e));
                    }
                }
            });
            
            // Make card images clickable
            document.querySelectorAll('.universe-card img').forEach(img => {
                img.style.cursor = 'pointer';
                img.addEventListener('click', function(e) {
                    e.stopPropagation();
                    const card = this.closest('.universe-card');
                    const title = card.querySelector('.card-title')?.textContent || '';
                    openImageModal(this.src, title);
                });
            });
        });

        // ===== MODAL FUNCTIONS =====
        function openModal(id) {
            document.getElementById(id).style.display = 'flex';
            document.body.style.overflow = 'hidden';
        }

        function closeModal(id) {
            document.getElementById(id).style.display = 'none';
            document.body.style.overflow = 'auto';
        }
        
        function openImageModal(imageSrc, title) {
            document.getElementById('modalImage').src = imageSrc;
            document.getElementById('modalImageTitle').textContent = title || '';
            openModal('imageModal');
        }

        // ===== CLOSE MODAL WHEN CLICKING OUTSIDE =====
        window.addEventListener('click', function(event) {
            const modals = document.querySelectorAll('.modal');
            modals.forEach(modal => {
                if (event.target === modal) {
                    closeModal(modal.id);
                }
            });
        });

        // ===== CLOSE MODAL WITH ESCAPE KEY =====
        document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
                const modals = document.querySelectorAll('.modal');
                modals.forEach(modal => {
                    if (modal.style.display === 'flex') {
                        closeModal(modal.id);
                    }
                });
            }
        });

        // ===== HIGHLIGHT ACTIVE NAV LINK =====
        const currentPath = window.location.pathname;
        document.querySelectorAll('nav .magic-btn').forEach(link => {
            if (link.getAttribute('href') === currentPath || 
                (link.getAttribute('href') === '{{ path('home') }}' && currentPath === '/') ||
                (link.getAttribute('href') === '{{ path('universe_index') }}' && currentPath.includes('/universes'))) {
                link.classList.add('active');
            }
        });
>>>>>>> afea128 (generation des avatars)
    </script>
</body>
</html>
